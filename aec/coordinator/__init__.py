"""Research coordinator package (EPIC 5 Part 1).

The offline end-to-end workflow: ingest Watch research candidates and
drive them through surface → memory → intelligence → assignment →
case → draft → plan → gate → evidence → review → report, using each
component's public contract. No component logic is duplicated here.
"""

__all__: list[str] = []
