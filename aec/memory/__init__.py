"""Case memory package (EPIC 3 Part 3).

Append-only per-case recall: observations, failed approaches, gap notes,
and reusable patterns. Every detail passes through the T2 redaction
scrubber before storage, so secret-shaped text is scrubbed at the door
and the stored record notes that it happened. No network, no writes.
"""

__all__: list[str] = []
